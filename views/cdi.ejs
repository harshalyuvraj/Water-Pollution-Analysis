<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chemical Data Information</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #2F4F4F;
            --primary-light: #3a5f5f;
            --secondary: #778899;
            --accent: #20B2AA;
            --light-bg: #f5f5f5;
            --dark-text: #2d3748;
            --border-color: #e0e0e0;
            --critical: #e53e3e;
            --warning: #dd6b20;
            --safe: #38a169;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background-color: var(--light-bg);
            color: var(--dark-text);
            margin: 0;
            padding: 20px;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
            padding: 25px;
            overflow-x: auto;
        }

        h2 {
            font-family: 'Poppins', sans-serif;
            color: var(--primary);
            margin-bottom: 25px;
            padding-bottom: 10px;
            border-bottom: 2px solid var(--border-color);
            font-weight: 700;
            font-size: 28px;
            white-space: nowrap;
        }

        .back-button-container {
            margin-bottom: 25px;
        }

        .back-button {
            padding: 10px 20px;
            font-size: 16px;
            background: linear-gradient(135deg, var(--primary), var(--primary-light));
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 6px;
            box-shadow: 0 2px 10px rgba(47, 79, 79, 0.3);
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            font-weight: 500;
        }

        .back-button:hover {
            background: linear-gradient(135deg, var(--primary-light), var(--primary));
            box-shadow: 0 4px 15px rgba(47, 79, 79, 0.4);
            transform: translateY(-2px);
        }

        .back-button::before {
            content: "←";
            margin-right: 8px;
            font-size: 18px;
        }

        table {
            width: 100%;
            table-layout: fixed;
            border-collapse: collapse;
            margin: 25px 0;
            font-size: 16px;
        }

        th, td {
            padding: 12px 15px;
            border: 1px solid var(--border-color);
            text-align: left;
            vertical-align: top;
            word-wrap: break-word;
            overflow-wrap: break-word;
        }

        th {
            background-color: var(--primary);
            color: white;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 14px;
            letter-spacing: 0.5px;
            position: sticky;
            top: 0;
            white-space: nowrap;
        }

        /* Fixed column widths - adjusted for chemical parameters */
        th:nth-child(1), td:nth-child(1) { width: 80px; }   /* Serial No */
        th:nth-child(2), td:nth-child(2) { width: 120px; }  /* Sample ID */
        th:nth-child(3), td:nth-child(3) { width: 100px; }  /* DO */
        th:nth-child(4), td:nth-child(4) { width: 100px; }  /* BOD */
        th:nth-child(5), td:nth-child(5) { width: 100px; }  /* COD */
        th:nth-child(6), td:nth-child(6) { width: 100px; }  /* NH₃ */
        th:nth-child(7), td:nth-child(7) { width: 100px; }  /* NO₃ */
        th:nth-child(8), td:nth-child(8) { width: 100px; }  /* NO₂ */
        th:nth-child(9), td:nth-child(9) { width: 120px; }  /* Phosphate */
        th:nth-child(10), td:nth-child(10) { width: 120px; } /* Chlorite */
        th:nth-child(11), td:nth-child(11) { width: 140px; } /* Date */

        tr:nth-child(even) {
            background-color: #f8fafc;
        }

        tr:hover {
            background-color: #f1f5f9;
        }

        .text-content {
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            line-clamp: 3;
            overflow: hidden;
            text-overflow: ellipsis;
            max-height: 4.5em;
            line-height: 1.5em;
        }

        .no-data {
            text-align: center;
            padding: 20px;
            color: var(--secondary);
            font-style: italic;
        }

        /* Value indicators */
        .value-critical {
            color: var(--critical);
            font-weight: 700;
            background-color: rgba(229, 62, 62, 0.1);
        }

        .value-warning {
            color: var(--warning);
            font-weight: 600;
        }

        .value-safe {
            color: var(--safe);
            font-weight: 600;
        }

        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }
            
            th, td {
                padding: 8px 10px;
                font-size: 14px;
            }
            
            /* Adjust column widths for mobile */
            th:nth-child(1), td:nth-child(1) { width: 60px; }
            th:nth-child(2), td:nth-child(2) { width: 100px; }
            th:nth-child(11), td:nth-child(11) { width: 120px; }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Back to Home Button -->
        <div class="back-button-container">
            <button class="back-button" onclick="goBackHome()">Back to Home</button>
        </div>

        <h2>Chemical Data Records</h2>
        <table>
            <thead>
                <tr>
                    <th>No.</th>
                    <th>Sample ID</th>
                    <th>DO (mg/L)</th>
                    <th>BOD (mg/L)</th>
                    <th>COD (mg/L)</th>
                    <th>NH₃ (mg/L)</th>
                    <th>NO₃ (mg/L)</th>
                    <th>NO₂ (mg/L)</th>
                    <th>Phosphate (mg/L)</th>
                    <th>Chlorite (mg/L)</th>
                    <th>Date</th>
                </tr>
            </thead>
            <tbody>
                <% if (typeof chemicalData !== 'undefined' && chemicalData.length > 0) { %>        
                    <% chemicalData.forEach((data,index) => { %>
                        <tr>
                            <td><div class="text-content"><%= index + 1 %></div></td>
                            <td><div class="text-content"><%= data.sampleID %></div></td>
                            <td class="chemical-value" data-type="do" data-value="<%= data.do %>">
                                <div class="text-content"><%= data.do %></div>
                            </td>
                            <td class="chemical-value" data-type="bod" data-value="<%= data.bod %>">
                                <div class="text-content"><%= data.bod %></div>
                            </td>
                            <td class="chemical-value" data-type="cod" data-value="<%= data.cod %>">
                                <div class="text-content"><%= data.cod %></div>
                            </td>
                            <td class="chemical-value" data-type="nh3" data-value="<%= data.nh3 %>">
                                <div class="text-content"><%= data.nh3 %></div>
                            </td>
                            <td class="chemical-value" data-type="no3" data-value="<%= data.no3 %>">
                                <div class="text-content"><%= data.no3 %></div>
                            </td>
                            <td class="chemical-value" data-type="no2" data-value="<%= data.no2 %>">
                                <div class="text-content"><%= data.no2 %></div>
                            </td>
                            <td class="chemical-value" data-type="phosphate" data-value="<%= data.phosphate %>">
                                <div class="text-content"><%= data.phosphate %></div>
                            </td>
                            <td class="chemical-value" data-type="chlorite" data-value="<%= data.chlorite %>">
                                <div class="text-content"><%= data.chlorite %></div>
                            </td>
                            <td><div class="text-content"><%= new Date(data.date).toDateString() %></div></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr><td colspan="11" class="no-data">No chemical data available</td></tr>
                <% } %>
            </tbody>
        </table>
    </div>

    <script>
        function goBackHome() {
            window.location.href = '/information';
        }

        document.addEventListener('DOMContentLoaded', function() {
            // Apply classification
            const chemicalValues = document.querySelectorAll('.chemical-value');
            
            chemicalValues.forEach(cell => {
                const value = parseFloat(cell.getAttribute('data-value')) || 0;
                const type = cell.getAttribute('data-type');
                let className = '';
                
                if (type === 'do') {
                    className = value < 2 ? 'value-critical' : value < 4 ? 'value-warning' : 'value-safe';
                } else if (type === 'bod') {
                    className = value > 30 ? 'value-critical' : value > 10 ? 'value-warning' : 'value-safe';
                } else if (type === 'cod') {
                    className = value > 100 ? 'value-critical' : value > 50 ? 'value-warning' : 'value-safe';
                } else if (type === 'nh3') {
                    className = value > 5 ? 'value-critical' : value > 2 ? 'value-warning' : 'value-safe';
                } else if (type === 'no3') {
                    className = value > 50 ? 'value-critical' : value > 25 ? 'value-warning' : 'value-safe';
                } else if (type === 'no2') {
                    className = value > 1 ? 'value-critical' : value > 0.5 ? 'value-warning' : 'value-safe';
                } else if (type === 'phosphate') {
                    className = value > 1 ? 'value-critical' : value > 0.5 ? 'value-warning' : 'value-safe';
                } else if (type === 'chlorite') {
                    className = value > 1 ? 'value-critical' : value > 0.5 ? 'value-warning' : 'value-safe';
                }
                
                if (className) {
                    cell.classList.add(className);
                }
            });

            // Make table rows interactive
            const rows = document.querySelectorAll('tbody tr');
            rows.forEach(row => {
                row.addEventListener('click', function() {
                    rows.forEach(r => r.classList.remove('active-row'));
                    this.classList.add('active-row');
                });
            });
        });
    </script>
</body>
</html>